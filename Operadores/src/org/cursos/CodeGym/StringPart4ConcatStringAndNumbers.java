package org.cursos.CodeGym;

/*
En la variable digits necesitas escribir la cadena "60".
Utilice las variables x, y, z, la cadena vacía y la cadena concatenada.

Requirements:
•	No cambies los valores de las variables x, y, z.
•	Para inicializar la variable de digits, use x, y, z, una cadena vacía y concatenación de cadenas.
•	El programa debería mostrar la variable de digits en la pantalla. 
*/

public class StringPart4ConcatStringAndNumbers {
    public static void main(String[] args) {
        int x = 2;
        int y = 4;
        int z = 0;

        String digits =(x + y) + "" + z; //escribe aquí tu código

        System.out.println(digits);
    }
}
